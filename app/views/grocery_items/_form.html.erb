<%= form_with(model: grocery_item) do |form| %>
  <% if grocery_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grocery_item.errors.count, "error") %> prohibited this grocery_item from being saved:</h2>

      <ul>
        <% grocery_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
